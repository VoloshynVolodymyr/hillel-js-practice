//task 4.1
let radius = 7;
let areaOfCircle = (Math.PI * Math.pow(radius, 2)).toFixed(2);

console.log(`Area of the circle with radius ${radius} equal : ${areaOfCircle}`);

//task 4.2
const length = 9;
const width = 3;

console.log('Area of ​​a rectangle : ', (length * width).toFixed(2));

//task 4.3
radius = 11;
let height = 4;
const areaOfCylinder = (Math.PI * Math.pow(radius, 2) * height).toFixed(2);
console.log(`Area of the cylinder with radius ${radius} and height ${height} equal : ${areaOfCylinder}`);